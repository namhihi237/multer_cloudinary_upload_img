doctype html
form(action='/upload', method='post', enctype='multipart/form-data')
  | Select image to upload:
  input#fileToUpload(type='file', name='image' multiple)
  input(type='submit', value='Upload Image', name='submit')
